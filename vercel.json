{
  "installCommand": "cd backend && npm ci --omit=dev",
  "buildCommand": "cd backend && npm run migrate",
  "functions": {
    "api/index.mjs": {
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.mjs"
    }
  ]
}
